{
  "name": "klima",
  "private": true,
  "workspaces": ["core", "web", "site"],
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '*' build"
  }
}

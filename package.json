{
  "name": "klima",
  "private": true,
  "workspaces": [
    "core",
    "server",
    "web",
    "site"
  ],
  "engines": {
    "node": "24.x"
  },
  "dependencies": {
    "bun": "^1.3.11"
  },
  "scripts": {
    "test": "bun test",
    "typecheck": "bun run --filter '*' typecheck",
    "build": "bun run --filter '*' build",
    "heroku-postbuild": "echo 'Rien a construire : le relais s execute depuis les sources TypeScript.'"
  }
}

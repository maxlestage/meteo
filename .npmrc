# Heroku construit avec NODE_ENV=production, et npm saute alors les
# dépendances de développement — dont Vite et TypeScript, sans lesquels il n'y
# a pas de site à servir. Elles sont élaguées après la construction, donc ça ne
# grossit pas ce qui tourne.
include=dev
